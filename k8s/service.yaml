apiVersion: v1
kind: Service
metadata:
  name: aks-mcp-server
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: aks-mcp-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
